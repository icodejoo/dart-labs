# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
# Libraries should not include pubspec.lock, per https://dart.dev/guides/libraries/private-files#pubspeclock.
/pubspec.lock
**/doc/api/
.dart_tool/
.flutter-plugins-dependencies
/build/
/coverage/

# NOTE: `prebuilt/` is intentionally NOT ignored. svgx distributes precompiled
# native binaries committed directly into git (no Git LFS — pub cannot resolve
# LFS pointers through its git checkouts). Ignoring it would silently break
# every consumer.
#
# 注意：`prebuilt/` 刻意不加入 ignore。svgx 直接把预编译原生二进制提交进 git
# （不用 Git LFS——pub 的 git checkout 拉不到 LFS 指针指向的实体文件）。
# 把它 ignore 掉会静默破坏所有下游。

# FVM Version Cache
.fvm/