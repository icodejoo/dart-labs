name: dioman
description: "Composable, self-contained Dio interceptor plugins (auth, cache, retry, dedup, mock, normalize, ...) plus the correct install order to wire them together."
version: 0.1.0
repository: https://github.com/icodejoo/dart-labs/tree/main/dioman
issue_tracker: https://github.com/icodejoo/dart-labs/issues

environment:
  sdk: ^3.5.0

dependencies:
  dio: ^5.0.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
