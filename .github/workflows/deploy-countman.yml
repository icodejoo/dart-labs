name: Deploy countman demo

on:
  push:
    branches: [main]
    paths:
      - 'countman/lib/**'
      - 'countman/example/lib/**'
      - 'countman/example/pubspec.yaml'
      - 'countman/pubspec.yaml'
      - '.github/workflows/deploy-countman.yml'
      - '.github/workflows/update-root-index.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: gh-pages
  cancel-in-progress: false

jobs:
  deploy:
    name: Build & deploy countman
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - name: Package dependencies
        working-directory: countman
        run: flutter pub get
      - name: Example dependencies
        working-directory: countman/example
        run: flutter pub get
      - name: Build web
        working-directory: countman/example
        run: flutter build web --release --base-href /dart-labs/countman/
      - name: Deploy countman/ to gh-pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: countman/example/build/web
          destination_dir: countman
          keep_files: true

  update-index:
    name: Update root index
    needs: deploy
    uses: ./.github/workflows/update-root-index.yml
    secrets: inherit
